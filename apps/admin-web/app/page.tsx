export default function AdminPlaceholder() {
  return (
    <main>
      <h1>Knowledge Explorer — Admin</h1>
      <p>
        Sign-in arrives with task 10; the authoring interface is P1 and beyond. Authorization is
        enforced by the API, never by hiding controls here (rule R-01).
      </p>
    </main>
  );
}
